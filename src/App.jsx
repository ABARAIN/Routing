import React from 'react';
import MapComponent from './MapComponent'; // Import the MapComponent

const App = () => {
  return (
    <div>
     
      {/* MapComponent displays the map and routes */}
      <MapComponent />
    </div>
  );
};

export default App;
